# API keys — config.toml [[keys]] keyEnv points at these variable names.
# Never commit the real .env file; only this example template.

CV_KEY_ADMIN=change-me-admin-key
CV_KEY_VIEWER=change-me-viewer-key

# Only needed when tagging.provider = "api" in config.toml:
# CV_TAGGING_API_KEY=sk-...